{{#if model}}
    <table class="table table-striped">
        <thead>
        <th>Name</th><th>Menu</th><th>Vote</th>
        </thead>
        <tbody>
        {{#each}}
          {{partial 'vote-partial'}}
        {{/each}}
        </tbody>
    </table>
{{else}}
    <p>Sorry no fixtures found for restaurants</p>
{{/if}}